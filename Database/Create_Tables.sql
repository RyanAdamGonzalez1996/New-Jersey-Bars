DROP TABLE bar_description;

CREATE TABLE bar_description (
    restaurant_name varchar   NOT NULL,
    bar_type varchar   NOT NULL,
    price_level varchar   NOT NULL,
    rating_list varchar   NOT NULL,
    address_list varchar NOT NULL,
);
